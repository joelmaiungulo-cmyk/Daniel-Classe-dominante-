from flask import Flask import os
app = Flask (__name__)
@app.route('/') def home():
  return ✈️ Daniel classe dominante Bot está online e pronto para operar 
if __name__ == '__main__':
  port = int(os.environ.get(' PORT', 5000)
             app.run( host='0.0.0.0'.pory=port)
